spring:
  autoconfigure:
    exclude:
      - org.springframework.ai.model.openai.autoconfigure.OpenAiAudioSpeechAutoConfiguration
      - org.springframework.ai.model.openai.autoconfigure.OpenAiAudioTranscriptionAutoConfiguration
      - org.springframework.ai.model.openai.autoconfigure.OpenAiChatAutoConfiguration
      - org.springframework.ai.model.openai.autoconfigure.OpenAiEmbeddingAutoConfiguration
      - org.springframework.ai.model.openai.autoconfigure.OpenAiImageAutoConfiguration
      - org.springframework.ai.model.openai.autoconfigure.OpenAiModerationAutoConfiguration

  threads:
    virtual:
      enabled: true
  application:
    name: oraculum

  datasource:
    url: jdbc:postgresql://${ORACULUM_DATABASE_HOST}:${ORACULUM_DATABASE_PORT}/${ORACULUM_DATABASE_NAME}
    username: ${ORACULUM_DATABASE_USERNAME}
    password: ${ORACULUM_DATABASE_PASSWORD}

  kafka:
    bootstrap-servers: ${ORACULUM_KAFKA_BROKERS}
    consumer:
      group-id: oraculum
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer

  flyway:
    enabled: true
    schemas: ${ORACULUM_FLYWAY_DEFAULT_SCHEMA}
    default-schema: ${ORACULUM_FLYWAY_DEFAULT_SCHEMA}

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false

vaadin:
  launch-browser: true

oraculum:
  data:
    refresh-enabled: true
    metadata:
      cron: "0 0 1 1 * *"
    company:
      cron: "0 0 2 * * TUE"
    fundamentals:
      cron: "0 0 2 * * TUE"
    share-price:
      incremental-window-days: 10
      cron: "0 0 3 * * TUE-SAT"
    news:
      incremental-window-hours: 1
      cron: "0 0 */4 * * *"

  database:
    host: ${ORACULUM_DATABASE_HOST}
    port: ${ORACULUM_DATABASE_PORT}
    name: ${ORACULUM_DATABASE_NAME}
    username: ${ORACULUM_DATABASE_USERNAME}
    password: ${ORACULUM_DATABASE_PASSWORD}
    maintenance:
      enabled: true
      vacuum-cron: "0 0 4 * * SUN"
      partition-cron: "0 0 5 1 * *"
      mv-refresh-cron: "0 0 4 * * TUE-SAT"

  kafka:
    consumer-group: oraculum
    topics:
      data-file-ready: oraculum.data_file_ready
      market: oraculum.market
      industry: oraculum.industry
      news: oraculum.news
      harvester-request: oraculum.harvester.request
      analyst-request: oraculum.analyst.request

  analyst:
    token-budget: 100000
    cleanup:
      data-cleanup-cron: ${ORACULUM_DATA_CLEANUP_CRON:0 3 * * *}
      data-retention-days: ${ORACULUM_DATA_RETENTION_DAYS:3}
    fact-sheet:
      history-limit: 3660
    share-price:
      monthly-history-limit: 3660
      daily-history-limit: 30
    news:
      history-limit: 30

  harvester:
    data-path: ${ORACULUM_HARVESTER_DATA_PATH:./data/harvester}
    data-cleanup:
      enabled: ${ORACULUM_HARVESTER_DATA_CLEANUP_ENABLED:true}
      retention-days: ${ORACULUM_HARVESTER_DATA_RETENTION_DAYS:1}
      cron: ${ORACULUM_HARVESTER_DATA_CLEANUP_CRON:0 0 2 * * *}

  llm:
    common:
      timeout: 120
      max-retries: 2
      retry-delay-ms: 500
      temperature: 0.0
      max-completion-tokens: 8192
      provider-fallback-order:
        - gemini
        - openai
        - groq

    providers:
      gemini:
        base-url: "https://generativelanguage.googleapis.com/v1beta/openai/"
        api-key: ${ORACULUM_GEMINI_API_KEY}

      groq:
        base-url: "https://api.groq.com/openai/v1"
        api-key: ${ORACULUM_GROQ_API_KEY}

      openai:
        base-url: "https://api.openai.com/v1"
        api-key: ${ORACULUM_OPENAI_API_KEY}

    models:
      mini:
        gemini: "gemini-3.1-flash-lite"
        groq: "llama-3.1-8b-instant"
        openai: "gpt-5.4-nano"

      standard:
        gemini: "gemini-3.5-flash"
        groq: "llama-3.1-70b-versatile"
        openai: "gpt-5.4-mini"

      pro:
        gemini: "gemini-3.1-pro-preview"
        groq: "llama-3.3-70b"
        openai: "gpt-5.4"